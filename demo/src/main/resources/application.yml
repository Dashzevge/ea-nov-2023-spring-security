
spring:
  application:
    name: test_project
  datasource:
    password: 99253188
    url: jdbc:mysql://localhost:3306/test_project
    username: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
server:
  port: 8080
